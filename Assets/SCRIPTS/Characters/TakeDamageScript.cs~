using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TakeDamageScript : MonoBehaviour
{

	public bool isHurt = false;
	public float hurtDuration = 0f;
	public float hurtDurationTimer = 0f;
	public float dashHurtDuration = 500f;
	public float jumpHurtDuration = 500f;

	void Start ()
	{
		
	}

	void Update ()
	{
		
	}

	void OnTriggerEnter2D (Collider2D target)
	{
		if (target.tag == "DashAttack" && this.tag == "Enemy" && !isHurt) {

		}
		if (target.tag == "JumpAttack" && this.tag == "Enemy" && !isHurt) {

		}
	}
}
